apiVersion: v1
kind: Service
metadata:
  name: {{ include "eventapp-lab08.fullname" . }}
  labels:
    {{- include "eventapp-lab08.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eventapp-lab08.selectorLabels" . | nindent 4 }}
