apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventapp-lab08.fullname" . }}-test-connection"
  labels:
    {{- include "eventapp-lab08.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eventapp-lab08.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
